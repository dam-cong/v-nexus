# Aggregate deps cho local dev (không dùng Docker). Mỗi service cũng có requirements.txt
# riêng dùng khi build image, để image nhỏ gọn hơn.
fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.40
mcp>=1.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.8
streamlit>=1.38
requests>=2.32

# tools/import_workbook_questions.py (đọc docs/*.xlsx)
openpyxl>=3.1

# dev/test
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
