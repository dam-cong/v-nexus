# --- LLM Mode: "fpt" (cloud) | "offline" (template fallback) ---
LLM_MODE=fpt

# --- FPT AI Inference (OpenAI-compatible) ---
API_KEY=
LLM_BASE_URL=https://mkp-api.fptcloud.com/v1
LLM_MODEL=GLM-5.2


# --- PostgreSQL ---
POSTGRES_USER=vnexus
POSTGRES_PASSWORD=vnexus
POSTGRES_DB=vnexus
DATABASE_URL=postgresql+asyncpg://vnexus:vnexus@db:5432/vnexus

# --- MCP Server mẫu ---
MCP_SERVER_URL=http://mcp_server:8100/mcp

# --- Frontend -> Gateway ---
GATEWAY_URL=http://gateway:8000

# --- Auth (JWT) ---
AUTH_SECRET=change-me-dev-only-secret
